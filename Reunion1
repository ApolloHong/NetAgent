Role: you are a presentation assistant. Produce a professional PowerPoint deck, in English, based on the attached materials.

Attachments I am providing:
- "Data Governance Script QA Lizhan Hong" (the speaking script; slide titles and content map one to one to it)
- "Trust Boundaries Data Flow Lizhan" (a PNG diagram of the three trust zones)
- "Architecture Security Data Governance Lizhan" (the architecture and security section, source for the status table)

Context: this deck supports a spoken review of about fifteen minutes with my supervisors and the deputy CISO at Orange. The audience is technical and security oriented. The agenda has six topics, and the deck ends with two access requests.

GENERAL STYLE RULES
- Light or white background on every slide. Never a dark background.
- Sober, professional, readable from a distance. One idea per slide.
- Very little text: at most three or four bullets per slide, each bullet short. No long sentences, no paragraphs. The slides support me, I develop the detail orally.
- Short, concrete titles. No marketing language, no buzzwords, no superlatives.
- Never use an em dash.
- Consistent readable font, 16:9 format, slide numbers on every slide.
- One single discreet accent colour, for example a sober blue or orange, used only for titles and key elements.
- Where a slide contrasts two things, use two columns rather than a paragraph.

FIGURE RULES
- Insert the provided PNG diagram "Trust Boundaries Data Flow" as an image, full width, on slide 7. Do not redraw it, do not simplify it, do not recreate it in native shapes.
- On slide 10, reuse the same diagram but cropped or zoomed to the lower portion showing the two control points and the blocked path. If cropping is not possible, insert the full diagram again at smaller size.
- Do not invent any other diagram or icon set. Simple boxes and arrows drawn natively are acceptable only where explicitly requested below.

SLIDE BY SLIDE SPECIFICATION

Slide 1 — Title
- Title: Data Governance and Compliance
- Subtitle: Network digital twin and operations agents
- Name: Lizhan Hong, Orange Wholesale (OW / OINIS)
- Date: July 2026
- No figure.

Slide 2 — What I will cover
- Title: Agenda
- Bullets: Data privacy and security policies / Anonymisation and synthetic data / Lab versus production scope / External API and export controls / Group security standards / Data handling and documentation
- Then, visually set apart at the bottom in the accent colour: Two access requests
- No figure.

Slide 3 — What the project touches
- Title: What the project touches, and what it does not
- Two columns.
  - Left column headed "Touches": network structural metadata (topology, configuration, routing state) / operational telemetry (counters, utilisation, errors, latency) / machine generated synthetic data
  - Right column headed "Does not touch": customer traffic content / subscriber data / billing and contractual data
- Below, one short line: classified Orange Restricted by default
- No figure.

Slide 4 — Principles
- Title: Four principles, each mapped to code
- Bullets: Data minimisation / Purpose limitation / Least privilege, read only on the twin / Offline by default, zero external calls
- No figure.

Slide 5 — Three layers of anonymisation
- Title: Three layers, not one
- Three short blocks or a simple three step native diagram, left to right:
  - Layer 1: the twin is a structural abstraction, no customer traffic
  - Layer 2: synthetic data, generated in the lab
  - Layer 3: pseudonymisation and minimisation before any model
- No external figure.

Slide 6 — Synthetic data
- Title: Synthetic means generated, not anonymised
- Bullets: Machine generated by the traffic model and fault injector / Deterministic and reproducible from a fixed seed / Not derived from real production traces
- Add one line visually marked as a limitation, in the accent colour: only three threshold breaches in the current window, not enough to validate alerting
- No figure.

Slide 7 — Three trust zones
- Title: Three zones, one directional flow
- Insert the provided "Trust Boundaries Data Flow" PNG, as large as possible.
- At most one short caption line: production is never entered by the agents
- Text must not compete with the figure on this slide.

Slide 8 — Invariants
- Title: Lab versus production, as invariants
- Bullets: No agent connection to production / The twin is a copy and carries no customer traffic / Live twin read only, experiments on a sandbox copy / Any device write gated behind an explicit flag, used only against the simulated twin
- No figure.

Slide 9 — Default state
- Title: The default configuration sends nothing
- Bullets: Rule based diagnosis, analytic model and evaluation all run locally / Zero external calls, no API key required / All results presented so far were produced in this state
- Make visually clear that this is the default, not a fallback.
- No figure.

Slide 10 — Two control points
- Title: Two control points, one path out
- Bullets: Control point 1, pseudonymisation and minimisation / Control point 2, egress gate classifying destination as local, internally hosted or external / External requires an explicit flag, off by default / Public or unknown destinations blocked by construction
- Figure: the cropped or reduced trust boundary diagram as specified in the figure rules.

Slide 11 — Audit and failure behaviour
- Title: Audited, and fail closed
- Bullets: Every attempt logged, allowed or blocked / Log records destination class, content hash, size, decision and reason, never the payload / On block, fall back to the local reasoner / A blocked egress reduces capability, it never leaks
- No figure.

Slide 12 — Enforced in code
- Title: Enforced by a test, not by a document
- Bullets: A static check asserts the model client is called in exactly one module / A self test command prints what would be sent and what is blocked / Available to run during this review
- Optionally show the command name in a monospace box: python -m demo security check
- No figure.

Slide 13 — Honest status
- Title: Implemented, designed, or a policy decision
- Present as a small three row table with these row labels and one short example each:
  - Implemented and running today
  - Control point, active when the optional layer is enabled
  - Deployment decision belonging to security policy
- Keep the table minimal, no more than two columns.
- No figure.

Slide 14 — Supply chain, model artefacts, accountability
- Title: Three shorter points
- Bullets: Core dependencies permissive, one copyleft component isolated in an optional extra / Model artefacts trained only on lab and synthetic data, no checkpoint has seen production configuration / Implementation and evidence mine, scope with supervisors, control validation with security
- No figure.

Slide 15 — Reproducibility
- Title: Reproducibility is a governance control
- Bullets: Virtual clock, fixed seeds, deterministic tie breaking / Results frozen with a manifest recording seed, configuration and window / Any number can be re-derived by someone else / If a result is disputed, we can tell whether data or code changed
- No figure.

Slide 16 — Lifecycle, exit, incidents
- Title: Lifecycle and exit
- Bullets: Extracts stay in the lab, never on a personal machine / Deleted at the end of the internship, credentials revoked / Handover is code, documentation and protocol, not data / On a suspected boundary crossing: stop, preserve the audit log, notify immediately
- No figure.

Slide 17 — Request one
- Title: Request 1, access to the internal model gateway
- Two columns.
  - Left headed "What and why": access to llmproxy from the work environment / the internally hosted path is the conservative option, it avoids external egress and third party retention / currently unavailable, which leaves either no model or a worse external option
  - Right headed "Boundaries": lab scope only / pseudonymised operational metadata only / every call through the egress gate and audited / fail closed to the local reasoner
- No figure.

Slide 18 — Request two
- Title: Request 2, a scoped labelled historical extract
- Two columns.
  - Left headed "What and why": per link counters, incident timestamps and recorded causes, for one zone / ground truth labels, to evaluate against reality instead of my own injected faults / statistical power, three breaches is not enough
  - Right headed "Boundaries": one zone, bounded window / operational metadata only, no customer identity / pseudonymised on ingest, no export / read only, deleted at end of internship
- Add one closing line: a pre pseudonymised or aggregated extract prepared by the data owner would also work
- No figure.

Slide 19 — Decisions requested
- Title: What I am asking you to decide
- Bullets: Access to the internal gateway / A scoped labelled extract / Boundaries can be tightened to whatever the guidelines require / Pointer to the formal process, and I follow up in writing
- End the slide with a plain "Thank you", no final full stop.
- No figure.

FINAL CONSTRAINTS
- Respect the order and the number of slides. Do not add content I have not asked for. Do not merge or split slides.
- Do not overfill any slide. If a slide would exceed four bullets, shorten the wording rather than adding a bullet.
- Keep a clean, uncluttered result on a light background, and use the provided PNG for slides 7 and 10.
